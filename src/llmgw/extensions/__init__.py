"""확장점(extension) 공개 계약과 로딩·실행 기반.

확장은 게이트웨이 프로세스 안에서 **신뢰된 코드로** 동작한다. 샌드박스가
아니다. 확장은 프롬프트와 응답을 읽을 수 있고, 태스크 역할 자격증명에
접근할 수 있고, 인터넷으로 데이터를 내보낼 수 있다. 따라서 설치와 활성화는
배포 운영자의 명시적 결정이어야 한다. 자세한 신뢰 경계는
`docs/extensions-v1.md` 에 있다.

공개 계약은 `llmgw.extensions.v1` 에만 있다. 게이트웨이 내부 모듈
(`schemas`, `domain`, `services`)은 확장에 노출하지 않는다. 내부 리팩터링이
확장을 깨뜨리지 않게 하려는 것이다.
"""
