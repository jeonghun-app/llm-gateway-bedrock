# 보안 정책

## 취약점 신고

보안 문제를 발견하면 **공개 이슈로 올리지 말고** 리포지토리 소유자에게 비공개로
연락한다. GitHub 의 Security → Report a vulnerability 기능을 쓸 수 있다.

---

## 시크릿 관리 원칙

이 리포지토리는 퍼블릭이다. 한 번 푸시된 값은 되돌릴 수 없다고 가정한다.
히스토리를 정리해도 클론·포크·캐시에 남는다.

### 저장되지 않는 것

| 값 | 처리 방식 |
|---|---|
| API 키 평문 | 저장하지 않는다. SHA-256 해시만 보관하고 발급 응답에서 한 번만 노출된다 |
| 관리 토큰 | CloudFormation 이 생성해 Secrets Manager 에만 둔다. 사람이 값을 정하지 않는다 |
| AWS 자격증명 | ECS 태스크 역할을 쓴다. 코드·환경변수 평문·이미지에 넣지 않는다 |
| 계정 ID·ARN | 런타임에 `sts get-caller-identity` 로 얻는다. 문서에는 플레이스홀더(`123456789012`)만 쓴다 |

### 커밋되지 않는 것

`.gitignore` 로 제외한다.

```
.deploy/          # 데모 API 키 평문 (파일 권한 600)
.env, .env.*
*.pem, *.key
credentials, secrets.json
```

`.deploy/` 아래 데모 키 파일은 시드 스크립트가 `chmod 600` 으로 만든다.
다른 경로로 옮기지 않는다.

---

## 시크릿 검사

커밋 전에 실행한다. CI 에서도 자동으로 돈다.

```bash
# 워킹트리와 커밋 메시지
./scripts/scan_secrets.sh

# 전체 커밋 히스토리까지 (느리다)
./scripts/scan_secrets.sh --history
```

검사 항목:

1. AWS 액세스 키(`AKIA`/`ASIA`), 시크릿 키 할당문
2. GitHub 토큰(`ghp_` 계열, `github_pat_`), Slack 토큰, OpenAI 키
3. 게이트웨이 API 키 평문(`sk-llmgw-*`), Bedrock API 키(`ABSK*`)
4. Private key 블록, JWT
5. 실제 AWS 계정 ID 하드코딩 (문서용 플레이스홀더는 예외)
6. 계정 ID 를 포함한 ARN
7. 추적돼서는 안 되는 경로(`.deploy`, `.env`, `credentials`, SSH 키)와 키 파일 확장자
8. 커밋 메시지

### 유출이 발생했다면

히스토리 정리만으로는 안전해지지 않는다. 순서를 지킨다.

1. **해당 자격증명을 즉시 폐기한다.** 이게 1순위다.
   - 게이트웨이 API 키: `DELETE /admin/accounts/{id}/keys/{key_id}`
   - 관리 토큰: 런북의 "관리 토큰 교체" 절차
   - AWS 키: IAM 에서 비활성화 후 삭제
2. CloudTrail 과 게이트웨이 사용량 기록으로 오용 여부를 확인한다.
3. 그다음에 히스토리를 정리한다.

---

## 네트워크 노출

### 접근 통제 구조

ALB 는 인터넷에 노출되고, 세 겹으로 제한한다.

1. **ALB 보안 그룹**이 관리형 프리픽스 리스트 하나만 참조한다. 리스트에 없는
   출처는 통과하지 못한다. **리스트는 빈 상태로 생성**되므로 아무도 접근할 수
   없는 상태에서 시작한다.
2. **태스크 보안 그룹**은 ALB 보안 그룹에서만 인바운드를 받는다. 태스크에 공인
   IP 가 붙어 있어도 인터넷에서 컨테이너 포트로 직접 접근할 수 없다.
3. **아웃바운드**는 443(AWS 엔드포인트)과 VPC 리졸버 DNS(53)로 제한한다.

### `0.0.0.0/0` 차단

세 지점에서 막는다.

- `scripts/deploy.sh` — `--allowed-cidr` 값을 검증한다. `/0` 프리픽스 거부
- `scripts/manage_access.sh` — 추가 시 검증한다. `/8` 보다 넓은 대역도 거부
- `scripts/smoke_test.sh` — 매 배포마다 프로젝트 보안 그룹에 `0.0.0.0/0`
  인바운드 규칙이 없는지 확인한다

상태 점검:

```bash
./scripts/manage_access.sh status
```

프리픽스 리스트 엔트리는 CloudFormation 이 관리하지 않으므로 AWS CLI 로 직접
넓은 대역을 넣는 것이 기술적으로 가능하다. 스모크 테스트의 검사가 그 상태를
잡아내는 마지막 그물이다.

### 알려진 위험

| 위험 | 영향 | 완화 |
|---|---|---|
| 인증서 없이 배포하면 HTTP 전용 | API 키와 관리 토큰이 평문 전송 | `--certificate-arn` 으로 HTTPS 활성화. **프로덕션 필수** |
| 태스크에 공인 IP | 보안 그룹 설정 실수가 곧 노출 | 인바운드가 ALB SG 로만 제한. 프라이빗 서브넷 전환은 ADR 0003 참고 |
| 관리 토큰 하나로 전체 계정 접근 | 토큰 유출 시 모든 테넌트 데이터 조회 가능 | 역할 분리는 미구현. 토큰 교체 절차를 런북에 문서화 |
| 대시보드가 인터넷 노출 | 토큰 없이는 데이터가 보이지 않으나 페이지 자체는 열린다 | 접근 CIDR 제한이 1차 방어 |

---

## 인증과 인가

### API 키

- 형식: `sk-llmgw-<env>-<43자 URL-safe 난수>`. 난수는 256비트
- 저장: SHA-256 해시. salt 를 쓰지 않는 이유는 키 자체가 256비트 난수라 사전
  공격 대상이 아니고, 인증 시 해시로 단일 조회를 해야 하기 때문이다
- 키 없음과 비활성 키를 **같은 메시지로 응답**한다. 존재 여부를 구분해 알려주면
  키 열거 공격의 신호가 된다
- 키는 캐시하지 않는다. 삭제가 즉시 적용된다

### 관리 토큰

- 40자 영숫자. CloudFormation 이 생성
- 비교는 `hmac.compare_digest` 로 타이밍 공격에 안전하게 처리
- **토큰이 설정되지 않으면 관리 API 가 503 을 반환한다.** 미설정을 무인증 허용으로
  해석하면 관리 API 가 인터넷에 열린다
- 브라우저는 `sessionStorage` 에만 보관한다. 탭을 닫으면 사라진다

### 캐시로 인한 반영 지연

계정·팀·사용자 상태는 30초 TTL 캐시를 거친다. 비활성화가 그만큼 늦게 적용된다.
**즉시 차단이 필요하면 계정 비활성화와 함께 키를 삭제한다.**

---

## IAM 최소 권한

태스크 역할과 실행 역할을 분리했다. 와일드카드가 불가피한 세 곳에는 템플릿에
사유를 주석으로 남겼다.

| 위치 | 이유 |
|---|---|
| `ecr:GetAuthorizationToken` | 리소스 단위 제한을 지원하지 않는 AWS 서비스 제약 |
| `bedrock:ListFoundationModels`, `ListInferenceProfiles` | 리소스 단위 제한 미지원. 읽기 전용이며 메타데이터만 반환 |
| 기반 모델 ARN | 게이트웨이 목적이 임의 모델 중계다. `AllowedBedrockModelArn` 파라미터로 좁힐 수 있다 |

신뢰 정책에 `aws:SourceAccount` 조건을 넣어 타 계정 사용을 막았다.

---

## 데이터 보호

| 항목 | 설정 |
|---|---|
| DynamoDB 저장 시 암호화 | SSE 활성화 (AWS 관리형 KMS 키) |
| DynamoDB PITR | 3개 테이블 모두 활성화 |
| ECR 이미지 암호화 | AES256 |
| ECR 이미지 스캔 | `scanOnPush` 활성화 |
| ECR 태그 | 불변(immutable). 배포된 이미지를 덮어쓸 수 없다 |
| 전송 중 암호화 | 인증서 지정 시 TLS 1.3/1.2 (`ELBSecurityPolicy-TLS13-1-2-2021-06`) |
| 로그 보존 | 명시적 설정 필수. 무한 보존은 선택할 수 없다 |
| usage 원본 TTL | 기본 90일 |

로그에 시크릿과 PII 를 남기지 않는다. 오류 응답은 AWS 에러 코드만 노출하고 원문
메시지는 로그에만 남긴다. `/readyz` 실패 응답도 예외 타입만 반환한다.

---

## 컨테이너

- 비root 실행 (UID/GID 10001 고정)
- 베이스 이미지를 태그와 다이제스트로 고정. 태그만 쓰면 재발행 시 빌드 결과가
  조용히 달라진다
- 2단계 빌드로 컴파일러와 pip 캐시를 최종 이미지에서 제외
- `.dockerignore` 로 `.git`, `.env`, 키 파일, 테스트, 문서 제외

---

## 프로덕션 전환 시 필수 항목

`docs/runbook.md` 의 체크리스트 중 보안 항목이다.

- [ ] ACM 인증서로 **HTTPS 활성화**. 선택이 아니다
- [ ] 접근 CIDR 을 최소로 좁힌다. `manage_access.sh list` 로 정기 점검
- [ ] DynamoDB `DeletionPolicy` 를 `Retain` 으로 변경
- [ ] `AllowedBedrockModelArn` 을 실제 사용 모델로 좁힌다
- [ ] 예산 한도 설정. 없으면 지출 상한이 없다
- [ ] `--alarm-email` 지정 후 확인 메일 승인
- [ ] CI/CD 는 OIDC 임시 자격증명 사용. 장기 액세스 키를 시크릿에 넣지 않는다
- [ ] 로그 보존 기간을 감사 요건에 맞게 조정
