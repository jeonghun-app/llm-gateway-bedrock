# 런타임 직접 의존성. 전이 의존성까지 포함한 전체 고정 목록은
# requirements.lock 에 있고, 컨테이너 빌드는 lock 파일을 사용한다.
#
# 버전은 모두 `==` 로 고정한다. 갱신 시 requirements.lock 도 함께 재생성한다:
#   ./scripts/lock_requirements.sh

fastapi==0.141.1
# starlette 는 fastapi 의 전이 의존성이지만 예외 기반 클래스를 직접
# import 하므로 직접 의존성으로 고정한다.
starlette==1.6.0
uvicorn==0.52.4
uvloop==0.22.1
httptools==0.8.0
pydantic==2.13.4
pydantic-settings==2.15.0
boto3==1.43.81
botocore==1.43.78
aws-lambda-powertools==3.34.0
# OIDC(Cognito 등) 액세스 토큰 검증. RS256 서명 확인에 cryptography 가 필요해
# extras 로 함께 고정한다.
pyjwt[crypto]==2.12.1
