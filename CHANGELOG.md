# 변경 이력

형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/) 를 따르고,
버전은 [유의적 버전](https://semver.org/lang/ko/)을 쓴다.

## [1.13.1] - 2026-08-30

### 수정

- **템플릿이 CloudFormation 인라인 한도를 넘어 배포가 실패했다.** 본문으로
  직접 넘기는 템플릿은 51,200 바이트로 제한된다. `infra/app.yaml` 이 v1.13.0
  에서 52,467 바이트가 되어 1,267 바이트 초과했다.

  1,267 바이트를 깎는 대신 S3 를 경유하도록 바꿨다. v1.12.1 이 51,125 바이트로
  **여유가 75 바이트뿐이었다.** 깎아도 다음 기능에서 바로 다시 막힌다. S3
  경유는 한도가 1MB 다.

  `deploy.sh` 가 템플릿 버킷을 필요할 때 만든다. 퍼블릭 액세스를 차단하고
  SSE-S3 를 켜고 30일 수명 규칙을 둔다. 버킷은 CloudFormation 이 관리하지
  않는다. 스택을 만들기 위해 필요한 버킷을 그 스택이 만들 수는 없어서다.
  `teardown.sh --purge-data` 가 함께 지운다.

## [1.13.0] - 2026-08-30

**확장점(extension)을 공개했다.** 요청 처리 경로에 자체 코드를 끼워 넣을 수
있다. 지금까지는 기능을 추가하려면 포크해야 했다.

### 추가

- **`RequestFilter` 확장점.** `llmgw.extensions.v1` 이 공개 계약이다. 동기
  `Protocol` 이고, 인증·레이트리밋·단가정책·모델권한·예산 검사를 모두 통과한
  뒤 **Bedrock 호출 전에** 실행된다. 따라서 거부해도 비용이 발생하지 않는다.
  - **내부 스키마를 노출하지 않는다.** frozen DTO(`RequestPayload`,
    `RequestContext`, `Message`, `ExtensionPrincipal`)를 따로 뒀다. 확장이
    제자리에서 수정하면 실행 순서에 따라 결과가 달라지고 원본을 알 수 없어
    감사가 불가능해진다. 내부 리팩터링이 확장을 깨뜨리는 것도 막는다.
  - **모델과 스트리밍 여부는 바꿀 수 없다.** 둘은 `RequestContext` 에만 있고
    반환 DTO 에는 없다. 바꿀 수 있으면 이미 통과한 권한 검사를 우회한다.
  - **키 해시·예산·허용 모델은 확장에 넘기지 않는다.** 정책 판단에 쓸 이유가
    없고, 넘기면 공개 계약이 내부 인증 모델과 결합된다.
- **fail-closed 실패 정책.** 확장이 예외를 던지거나 제한 시간(기본 1초)을
  넘기거나 잘못된 형식·빈 대화를 반환하면 503 이고 Bedrock 을 호출하지 않는다.
  개인정보 마스킹이 고장났을 때 요청을 흘려보내면 확장을 켠 의미가 없다.
  - 확장이 던진 예외 문구는 클라이언트로 나가지 않는다. 프롬프트 조각이나
    자격증명이 섞일 수 있다. 확장이 임의 HTTP 상태 코드를 고르는 것도 막는다.
  - 제한 시간을 넘긴 확장은 차단 상태가 되어 이후 요청이 즉시 실패한다. 워커가
    묶인 상태로 계속 작업을 넣으면 큐만 쌓인다.
  - **제한 시간은 샌드박스가 아니다.** 파이썬 스레드는 강제로 멈출 수 없다.
    "늦게 온 결과를 쓰지 않는다" 는 보장일 뿐이다. 문서에 명시했다.
- **명시적 활성화.** `LLMGW_REQUEST_FILTERS` 에 `module:Class` 로 나열한 것만
  import 하고 실행한다. 나열하지 않은 모듈은 import 조차 하지 않는다.
  `entry_points` 자동 발견을 쓰지 않은 이유는, 파이썬 모듈이 import 만으로
  최상위 코드를 실행하고 확장이 프롬프트와 자격증명에 접근할 수 있어서다.
  의존성을 하나 추가한 것이 곧 그 권한을 준 것이 되면 안 된다.
- **로딩 실패는 기동 실패.** 설정한 확장을 못 불러오면 게이트웨이가 시작하지
  않는다. 확장 없이 기동하면 운영자는 필터가 동작한다고 믿는 상태에서 필터
  없이 트래픽을 받는다.
- **참조 확장 2개** (`examples/extensions/request_filters.py`). 변형 경로와
  거부 경로를 각각 검증한다. 표준 라이브러리만 쓴다.
- 배포 옵션 `--request-filters`, CloudFormation 파라미터 `RequestFilters` ·
  `ExtensionTimeoutSeconds`.
- [`docs/extensions-v1.md`](docs/extensions-v1.md) — 계약, 신뢰 경계, 실패
  정책, 버전 정책, 설치 절차.

### 수정

- **`chat_completions` 의 `x_request_id` 설명이 1.4.0 에서 폐기한 멱등성을
  여전히 설명하고 있었다.** "재시도 시 같은 값을 보내면 사용량이 중복 집계되지
  않는다" 고 적혀 있었는데, 1.4.0 에서 정렬 키를 `usage_id` 로 바꾼 뒤로는
  호출 횟수만큼 집계된다. `usage.py` 와 README 는 맞게 적혀 있었고 이 docstring
  만 남아 있었다. 확장 구현자가 `request_id` 를 멱등 키로 잘못 쓰게 만들 수
  있어 고쳤다. 멱등 키는 `usage_id` 다.

### 알려진 제약

- **자체 확장을 쓰려면 파생 이미지를 빌드해야 한다.** 임의의 파이썬 코드를 이미
  만들어진 이미지에 런타임으로 붙이는 방법은 없다. v1.11.0 의 "Docker 불필요"
  이점은 확장을 쓰지 않을 때만 유지된다. 문서에 명시했다.
- **`ResponseFilter` 와 `UsageSink` 는 아직 없다.** 응답 필터는 스트리밍에서
  이미 보낸 청크를 회수할 수 없는 근본 문제가 있어 계약을 확정하지 못했다.
  현재 방향은 응답 필터가 활성이면 스트리밍 요청을 명시적으로 거부하는 것이다.
  사용량 싱크는 전달 보장 수준을 먼저 정해야 한다.

## [1.12.1] - 2026-08-30

### 수정

- **스모크 테스트를 데모 키 없이 실행하면 항상 1건 실패했다.**
  `LLMGW_DEMO_KEY_FILE` 은 기본값이 없어, README 가 안내하는 수동 실행
  (`LLMGW_BASE_URL=... ./scripts/smoke_test.sh`)은 임시 계정 경로를 탄다. 그
  경로가 팀을 만들지 않고 사용자를 `team_id` 없이 생성해 team 축 집계가 비었고,
  집계 검사가 구조적으로 통과할 수 없었다. `deploy.sh` 는 이 변수를 넘기므로
  배포 중에는 드러나지 않았다.

  임시 계정 경로에서도 팀을 만들고 사용자를 넣도록 고쳤다. 검사를 지우는 대신
  팀을 만든 이유는 팀별 비용 귀속이 이 게이트웨이의 핵심 기능이라서다. 이제
  수동 실행 경로도 팀 단위 집계를 실제로 검증한다.

## [1.12.0] - 2026-08-30

**태스크를 프라이빗 서브넷에 둘 수 있게 했다.** 태스크가 공인 IP 를 가진다는
점이 보안 정책에 걸려 도입하지 못하는 조직이 있었다.

### 추가

- **`--task-subnet-mode private-nat`.** 프라이빗 서브넷 2개와 NAT Gateway 를
  만들고 태스크의 `AssignPublicIp` 를 `DISABLED` 로 둔다. 기본값 `public` 은
  기존과 같아 추가 비용이 없다.
  - 게이트웨이 VPC 엔드포인트(DynamoDB·S3)를 프라이빗 라우트 테이블에도
    연결했다. 연결하지 않으면 이 트래픽이 NAT 를 지나 데이터 처리비가 붙는다.
  - NAT Gateway 는 한 개만 만든다. AZ 마다 두면 비용이 두 배가 되고, ALB
    인바운드는 NAT 를 타지 않으므로 영향이 태스크 아웃바운드에 한정된다.
  - **인터페이스 VPC 엔드포인트 방식은 넣지 않았다.** 필요한 5종
    (`bedrock-runtime`, `ecr.api`, `ecr.dkr`, `secretsmanager`, `logs`)을 2개 AZ 에
    두면 월 약 73 USD 로 NAT(약 33 USD)보다 비싸다. 산정 근거를 README 에 적었다.
- **이미지 provenance 검증 절차.** `gh attestation verify` 로 받은 이미지가 이
  리포지토리의 CI 에서 나왔는지 확인하는 방법을 문서화했다. 다이제스트 고정
  배포 예시도 함께 넣었다.

### 수정

- **`EcrRepositoryArn` 을 빈 값으로도 반드시 전달한다.** 전달하지 않으면
  CloudFormation 이 기존 값을 유지하므로, private ECR 로 배포했던 스택을 공개
  이미지로 옮길 때 태스크 실행 역할에 ECR pull 권한이 남았다. 배포된 환경에서
  실측으로 확인하고 고쳤다. 수정 후 정책이
  `WriteOwnLogGroup`·`ReadAdminTokenSecret` 2개로 줄어드는 것과, ECR 권한 없이도
  GHCR 익명 pull 로 태스크가 정상 기동하는 것을 확인했다.

## [1.11.0] - 2026-08-29

**BYOC(고객 자기 계정 배포)를 1차 배포 모델로 확정하고, Docker 없이 설치할 수
있게 했다.** 기업에서 개발자 PC 의 이미지 빌드를 막는 경우가 많아 이것이 가장
큰 도입 장벽이었다.

### 추가

- **사전 빌드 이미지로 설치.** `--image` 옵션을 주면 ECR 스택 생성과 로컬
  Docker 빌드를 건너뛴다. `docker` 검사도 하지 않는다.
- **GHCR 이미지 발행 워크플로.** `v*` 태그를 밀면 GitHub Container Registry 에
  공개 이미지를 올린다.
  - GHCR 을 고른 이유: 소스가 이미 이 리포지토리에 있어 신뢰 경로가 짧고,
    `GITHUB_TOKEN` 으로 푸시하므로 CI 에 AWS 자격증명을 두지 않으며,
    provenance 증명을 GitHub 이 서명한다.
  - `latest` 태그를 붙이지 않는다. 고객이 `latest` 를 쓰면 태스크가 재기동될
    때마다 버전이 바뀔 수 있고 어떤 코드가 돌고 있는지 알 수 없다.
  - 태그와 코드 버전이 다르면 발행을 실패시킨다. 고객이 받는 이미지와 문서가
    어긋나는 것을 막는다.
- **`ImageUri` 에 GHCR 기본값.** 콘솔에서 파라미터를 비워도 배포된다.

### 변경

- **`EcrRepositoryArn` 을 선택 파라미터로 바꿨다.** 비우면 태스크 실행 역할에
  ECR pull 권한을 **부여하지 않는다.** 공개 레지스트리는 익명 pull 이라 권한이
  필요 없고, 주지 않는 편이 최소권한에 맞다. private ECR 을 쓸 때만 지정한다.
- **설치 경로를 세 가지로 문서화했다.** 사전 빌드 이미지(평가) / 계정 내
  private ECR(프로덕션) / 소스 빌드(기여).
  - 프로덕션에 private ECR 복사를 권하는 이유를 명시했다. Fargate 는 태스크를
    띄울 때마다 이미지를 새로 받으므로(호스트 캐시가 없다) 외부 레지스트리
    장애가 스케일아웃과 장애 복구를 막는다.
- **데이터 위치를 문서에 못박았다.** 레지스트리 위치가 배포 리전과 무관하고,
  게이트웨이·테이블·로그·Bedrock 호출이 모두 고객 계정 안에서 일어나며, 프롬프트
  본문은 저장하지 않는다는 점을 명시했다. BYOC 에서 데이터 위치는 도입 판단의
  핵심이다.

## [1.10.0] - 2026-08-29

단시간 폭주를 막는 레이트 리밋, API 키 만료, 단가 미등록 모델 정책을 추가했다.

**세 기능 모두 기본값이 기존 동작과 같다.** 한도 미설정·만료 미설정·`allow`
정책이 기본이므로, 설정하지 않은 배포는 동작이 바뀌지 않는다.

### 추가

- **분당 요청 한도(RPM).** 월 예산은 사후 통제라 폭주 루프가 몇 분 안에 큰
  비용을 만드는 것을 막지 못한다. 키(`rpm_limit`) 또는 사용자 단위로 걸고,
  더 좁은 범위인 키 한도가 사용자 한도를 덮어쓴다.
  - DynamoDB 조건부 업데이트(`ADD` + `ConditionExpression`)로 원자적으로
    센다. 태스크별 인메모리 카운터를 쓰면 한도가 태스크 수만큼 늘어나고
    오토스케일링으로 그 배수가 계속 바뀐다.
  - 새 테이블을 만들지 않고 usage 테이블의 다른 파티션 namespace 를 쓴다. 이
    테이블에는 이미 TTL 이 걸려 있어 만료된 분 단위 카운터가 자동으로 사라진다.
  - 파티션 키에 호출 주체가 들어가므로 특정 분에 부하가 몰리는 핫 파티션이
    생기지 않는다.
  - 한도가 없으면 DynamoDB 를 건드리지 않는다. 미설정이 기본값이라 요청 경로에
    왕복이 늘지 않는다.
  - 429 와 함께 `Retry-After` 를 내려보낸다. 없으면 OpenAI 클라이언트가 즉시
    재시도해 거부가 반복되고 서로 부하만 늘어난다.
  - **분당 토큰(TPM)은 넣지 않았다.** 요청 전에 출력 토큰 수를 알 수 없고
    `max_tokens` 는 선택 필드라 비어 있는 요청이 많다. 그 값으로 예약하면
    지정한 클라이언트만 불리해진다.
- **API 키 만료(`expires_at`).** 임시로 내주는 키를 사람이 기억하지 않아도
  회수된다. 만료된 키는 **삭제하지 않고 거부만** 한다. 지우면 사용량 집계의
  `KEY#<key_id>` 축에 이름을 붙일 수 없다. DynamoDB TTL 도 쓰지 않는다. TTL
  삭제는 최대 48시간 지연되어 그동안 만료된 키가 유효해진다.
  - 저장된 만료 시각이 깨져 있으면 만료로 다룬다. 파싱 실패를 "무기한 유효"
    로 해석하면 회수하려던 키가 영구히 살아남는다.
- **단가 미등록 모델 정책(`LLMGW_UNPRICED_MODEL_POLICY`).** `allow`(기본,
  기존 동작) / `reject`(거부해 비용 귀속 보장) / `hide`(`/v1/models` 에서만
  감춤) 중에 고른다.
  - 배포 실측에서 게이트웨이가 노출하는 101개 모델 중 61개에 단가가 없었다.
    단가가 없으면 비용이 0으로 집계되어 월 예산이 걸리지 않고 청구 배분에서
    빠진다.
  - **자동화로는 풀 수 없다.** `scripts/sync_pricing.py` 실행 결과 현재 표는
    AWS Price List API 와 정확히 일치했고(불일치 0건), 미등록 모델은 Price
    List API 에도 단가가 없었다. 가져올 원천이 없으므로 대신 운영자가 정책을
    고르게 했다.
- **`UnpricedRequests` EMF 메트릭과 CloudWatch 알람.** 이전에는 경고 로그만
  남아 아무도 보지 않았다.
- **관리 API 노출.** 키·사용자 생성·수정에서 `rpm_limit` 와 `expires_at` 를
  다룬다.
- **`--unpriced-model-policy` 배포 옵션.**

### 수정

- **`update_api_key` 가 새 필드를 저장하지 않던 문제.** 키 직렬화가 생성과
  갱신 두 곳에 중복돼 있어 한쪽만 고치면 조용히 누락된다. 이번에 추가한
  필드가 갱신 경로에서 사라지는 것을 테스트가 잡았다.

## [1.9.0] - 2026-08-29

대시보드에 **예산 소진 현황**을 추가했다. 이 제품의 핵심 가치가 비용 통제인데
화면에 예산이 전혀 보이지 않아 소진을 사후에만 알 수 있었다.

### 추가

- **예산 소진 블록.** 계정·팀·사용자·키 네 축의 이번 달 한도와 누적을 KPI
  바로 아래에 보여준다. 소진율이 높은 항목이 위에 온다.
  - 80% 이상은 주의, 100% 이상 또는 차단 상태는 위험으로 색과 문구를 함께
    바꾼다. 색만으로 구분하지 않는다.
  - 한도가 0 이면 비율을 계산하지 않고 "즉시 차단" 으로 표시한다. 0으로 나누는
    것을 피하고, 그 상태는 비율보다 상태로 표현하는 것이 정확하다.
  - 단가 표에 없는 모델이 섞인 축에는 실제 소진율이 더 높을 수 있다는 경고를
    함께 띄운다. 그 요청은 비용이 0으로 집계되기 때문이다.
  - 진행률 바에 `role="progressbar"` 와 값을 붙여 스크린리더에서도 읽힌다.
  - 예산을 지정하지 않은 항목은 담지 않는다. 무제한이라 소진율이 정의되지
    않는다.
- **`/analytics/dashboard` 응답에 `budgets` 블록.** 조회 기간과 무관하게 항상
  이번 달을 본다. 예산은 월 단위로 강제되고 과거 예산 이력을 저장하지 않으므로,
  지난달 소진율을 현재 한도로 재구성하면 사실과 다른 값이 된다.

### 테스트

- 예산 블록 검증 4건: 예산이 설정된 축만 담기는지, 예산이 없으면 비는지, 한도
  0 이 차단으로 표시되는지, 소진율 높은 순으로 정렬되는지.
- i18n 정적 검사를 개선했다. 줄바꿈된 `t()` 호출과 번역 키를 돌려주는 함수를
  오탐하던 문제를 고쳤다.

## [1.8.0] - 2026-08-29

대시보드를 한국어와 영어로 쓸 수 있게 하고, 문서를 두 언어로 제공한다.

### 추가

- **대시보드 언어 전환.** 사이드바에서 한국어와 영어를 고른다. 선택은
  브라우저에 저장되어 다음 방문에도 유지된다. 시각과 숫자 표기도 언어에 맞는
  로케일을 따른다(영어 화면에 `오후 3:26` 이 나오지 않는다).
  - 원문(한국어)을 키로 쓴다. 키를 따로 만들면 번역이 빠졌을 때 화면에 내부
    식별자가 노출되는데, 이 방식은 원문으로 안전하게 대체된다.
  - 정적 텍스트는 `data-i18n` 속성으로, 동적으로 그려지는 표·차트·관리 화면은
    다시 렌더링해 반영한다.
  - 외부 i18n 라이브러리를 쓰지 않는다. 사설망에서 CDN 없이 동작해야 한다.
- **영문 README.** `README.en.md` 를 추가하고 두 문서 상단에 언어 전환 링크를
  넣었다.
- **언어별 대시보드 스크린샷.** 각 언어 문서에 데이터가 있는 상태의 화면을
  넣었다(`docs/images/dashboard-ko.png`, `dashboard-en.png`).

### 수정

- **표 헤더와 캡션이 언어 전환에 반응하지 않던 문제.** 열 정의와 캡션이 모듈
  로드 시점에 한 번 평가되는 상수여서, 언어를 바꿔도 표만 한국어로 남았다.
  함수로 바꿔 렌더링 시점에 평가하게 했다.
- **시크릿 스캐너가 링크로컬 주소를 공인 IP 로 오탐하던 문제.** IMDS
  (`169.254.169.254`)만 예외였고 Fargate 태스크 자격증명 주소
  (`169.254.170.2`)는 걸렸다. 두 주소는 SSRF 방어 대상으로 코드와 문서에
  명시해야 하고, 공표된 고정 주소라 노출해도 공격 표면이 되지 않는다.

### 테스트

- i18n 회귀 테스트 4건. 화면에 쓰이는 모든 한국어 문자열이 사전에 있는지,
  `t()` 로 감싸지지 않은 리터럴이 없는지 정적으로 검사하고, 실제 브라우저에서
  언어 전환 시 정적 텍스트와 동적 표 헤더가 함께 바뀌는지, 모니터링·상세
  6탭·관리 5탭을 순회해 미번역이 없는지 확인한다.
- Node 하네스에 i18n 스텁을 주입했다. 하네스는 렌더링 로직만 검증한다.

## [1.7.0] - 2026-08-29

고객이 이미 쓰는 인증 서버(Amazon Cognito, Okta, Azure AD, Google)를 계정 단위로
붙일 수 있게 했다. 공유 관리 토큰 하나로만 관리하던 구조를 계정 방식으로 넘길 수
있다.

기존 API 키와 공유 관리 토큰은 그대로 동작한다. OIDC 를 설정하지 않으면 동작
변화가 없다.

### 추가

- **계정별 외부 인증(OIDC) 연동.** 발급자(`iss`)로 토큰이 어느 계정 것인지
  판별하고 그 계정에 등록된 설정만 사용한다. 전역 설정을 두지 않은 이유는 두 곳에
  같은 값이 있으면 어느 쪽이 적용됐는지 추적하기 어렵고 한쪽만 고쳐 사고가 나기
  때문이다.
  - 발급자는 계정 간에 겹칠 수 없다. 조건부 쓰기로 다른 계정이 쓰는 발급자를
    가로채지 못하게 한다. 가로챌 수 있으면 그 계정 사용자로 위장할 수 있다.
  - 설정 본문과 발급자 역인덱스를 하나의 트랜잭션으로 쓴다.
- **`/v1` 에서 OIDC 토큰을 받는다.** `Authorization: Bearer` 값이 `sk-llmgw-` 로
  시작하면 API 키, 그 외는 JWT 다. 예산·모델 제한은 API 키 경로와 동일하게
  적용된다. 실패 메시지는 한 가지로 통일해 어느 쪽이 틀렸는지 알려주지 않는다.
- **관리 API 를 계정 방식으로 쓸 수 있다.** 토큰의 그룹이 계정 설정의
  `admin_groups` 에 있으면 그 계정만, 전역 `LLMGW_OIDC_PLATFORM_ADMIN_GROUPS` 에
  있으면 모든 계정을 관리한다. 공유 토큰은 부트스트랩과 비상 접근용으로 남는다.
  - 권한 범위는 `require_admin` 한 곳에서 경로의 `account_id` 로 강제한다. 관리
    엔드포인트가 23개인데 각각에서 검사하면 새 엔드포인트가 빠뜨릴 수 있다.
  - 계정 생성은 계정을 본문으로 받아 경로 검사가 통하지 않으므로 플랫폼 범위만
    허용한다.
  - 관리 API 인증 성공 시 주체·범위·계정을 로그에 남긴다. 공유 토큰으로는 누가
    했는지 알 수 없었다.
- **셀프서비스 키 발급(`/auth`).** 개발자가 관리자에게 요청하지 않고 스스로 키를
  받는다. `GET /auth/me` 로 클레임 매핑을 확인하고, `POST /auth/keys` 로 본인
  키를 발급한다. `GET`/`DELETE /auth/keys` 로 자기 키만 조회·폐기한다.
- **관리 UI "인증 연동" 탭.** 계정별 OIDC 설정을 화면에서 만들고 수정하며, 설정을
  지우지 않고 즉시 차단·재허용할 수 있다. IdP 쪽 문제에 급히 대응하고 되돌릴 수
  있어야 한다.
- **`LLMGW_OIDC_PLATFORM_ADMIN_GROUPS`** 환경변수. 이 그룹에 속한 토큰은 모든
  계정을 관리한다. 비어 있으면 토큰으로 플랫폼 관리자를 부여할 수 없다.

### 보안

- **JWKS URL 을 검증한다.** `jwks_url` 은 관리자가 설정하는 값이라 검증 없이
  호출하면 게이트웨이가 내부망으로 요청을 보내는 SSRF 통로가 된다. Fargate 에서는
  `169.254.170.2` 가 태스크 역할 자격증명을 서빙한다. https 만 허용하고, 호스트명을
  실제 해석해 사설·링크로컬·루프백·예약 대역이면 거부한다. 이름만 보면 내부
  주소로 해석되는 우회를 막을 수 없다. 요청 직전에 재검증해 DNS 리바인딩도 막는다.
- **비대칭 서명만 허용한다.** `HS*` 와 `none` 을 거부해 `alg` 바꿔치기를 막는다.
- **자동 생성에 예산을 강제한다.** 예산 없이 켜면 IdP 계정만 있는 사람이 무제한으로
  Bedrock 을 호출하고 비용은 계정 소유자가 부담한다. 설정 단계에서 막는다.
- **셀프서비스에서 권한 상승이 불가능하다.** 계정·사용자를 본문으로 받지 않아 남에게
  키를 발급할 수 없고, 허용 모델은 계정 범위와 교집합만 남으며, 예산은 요청으로
  지정할 수 없다. 남의 키는 존재 여부도 알려주지 않는다.

### 수정

- **계정 삭제 시 발급자 역인덱스가 고아로 남던 문제.** 인증 설정을 남긴 채 계정을
  지우면 다른 계정이 그 발급자를 영구히 쓸 수 없게 된다. 기존 참조 무결성 정책과
  같게 거부한다.
- **`repository` 모듈 설명의 옛 멱등성 서술.** 1.4.0 에서 정렬 키를 `usage_id` 로
  바꿨는데 모듈 docstring 이 갱신되지 않았다.

## [1.6.0] - 2026-08-29

대시보드 정보 구조를 다시 짰다. 1.5.0 은 Flowbite 색·반경·그림자 같은 토큰만
적용했고 화면 구조는 그대로 세로 적층이라, 대시보드가 아니라 스타일만 입힌 폼
페이지처럼 보였다. API 와 데이터 모델은 바뀌지 않는다.

### 변경

- **사이드바 셸 레이아웃 도입.** 좌측 고정 사이드바(브랜드 + 화면 전환)와 본문
  영역으로 나눴다. 화면 전환 탭을 사이드바로 옮겨 아이콘과 함께 놓았다.
- **상단 바 분리.** 페이지 제목과 관리 토큰을 상단 바에, 조회 조건(계정·기간·
  프리셋·조회·자동 새로고침)을 그 아래 툴바에 배치했다. 이전에는 토큰 입력폼이
  페이지 최상단을 차지했다.
- **KPI 카드에 아이콘을 넣었다.** 지표별 색상 아이콘으로 시각적 기준점을 준다.
  아이콘은 인라인 SVG 라 외부 요청이 없다.
- **차트 배치를 6열 그리드로 밀도 있게 바꿨다.** 시계열이 4칸, 도넛이 2칸으로
  한 행에 나란히 놓이고, 그 아래 팀별·사용자별 막대 차트가 3칸씩 절반을 쓴다.
  이전에는 시계열이 전체 폭을 혼자 쓰고 나머지가 3등분되어 빈 공간이 컸다.
- **섹션 제목을 작은 대문자 라벨로 낮췄다.** 카드 제목이 이미 내용을 말하므로
  세로 잡음을 줄였다.

### 수정

- **넓은 카드에서 차트 글자가 비정상적으로 확대되던 문제.** `viewBox` 로 그린
  SVG 에 `width: 100%` 만 주면 카드가 넓어질수록 세로도 같이 커진다. 최대
  높이를 두어 가로로만 여유를 준다.
- **좁은 화면에서 표 글자가 한 글자씩 세로로 쪼개지던 문제.** 표에 최소 폭을
  주고 감싼 요소가 가로 스크롤을 담당하게 했다.
- **짧은 차트 아래에 죽은 공간이 남던 문제.** 카드가 행 높이에 맞춰 늘어나지
  않도록 정렬을 바꿨다.

## [1.5.1] - 2026-08-29

배포해도 대시보드 디자인이 바뀌지 않던 문제를 고친 패치 릴리스다.

### 수정

- **대시보드 애셋이 배포 후에도 갱신되지 않던 문제.** `/ui/` 정적 응답에
  `Cache-Control` 헤더가 없어 브라우저가 휴리스틱 캐싱을 적용했다. 새 CSS·JS
  를 배포해도 브라우저가 옛 사본을 계속 써서, 서버에는 새 파일이 올라갔는데
  화면은 그대로인 상태가 됐다. `/ui` 이하 응답에 `Cache-Control: no-cache` 를
  붙여 매번 재검증하게 했다. 캐시 금지가 아니라 "쓰기 전에 확인" 이므로
  내용이 그대로면 ETag 로 304 가 돌아가 전송량은 늘지 않는다.
  - 회귀 테스트 2건을 추가했다. `/ui/` 이하에는 재검증 헤더가 붙고, API
    경로에는 붙지 않는지 확인한다.
  - **이미 옛 사본을 캐시한 브라우저는 한 번 강제 새로고침이 필요하다**
    (Windows/Linux `Ctrl+Shift+R`, macOS `Cmd+Shift+R`). 이후로는 배포만
    하면 자동 반영된다.

## [1.5.0] - 2026-08-29

대시보드를 Flowbite 디자인 시스템으로 다시 만들었다. API 와 데이터 모델은
바뀌지 않는다.

### 변경

- **대시보드 UI 전면 재작업.** Flowbite(Tailwind 기반)의 디자인 토큰과 컴포넌트
  해부구조를 적용했다. 라이브러리를 가져오는 대신 같은 규격을 손으로 구현한다.
  Flowbite 는 Tailwind 유틸리티 클래스를 전제해 CDN 또는 npm 빌드 단계가
  필요한데, 이 컨테이너는 인터넷에 나가지 않고도 UI 가 온전히 동작해야 하고
  이미지 빌드에 npm 툴체인을 넣지 않는다는 결정을 유지해야 한다.
  - 색: Tailwind gray / blue 팔레트를 그대로 쓴다. 차트 시리즈도 같은 계열로
    맞췄다.
  - 컴포넌트: 카드(`rounded-lg` + `shadow-sm`), 표(회색 헤더 + 대문자
    `text-xs` 레이블), 배지, 버튼 그룹, 밑줄형 화면 전환 탭, 알약형 상세 탭,
    폼 입력, 모달을 Flowbite 규격에 맞췄다.
  - 상호작용 채움색을 텍스트·테두리용 색과 분리했다(`--btn-bg`). 다크 모드에서
    버튼이 파란 배경 + 흰 글자를 유지한다.
  - 상단 바를 `sticky` 로 두고 본문 최대 폭을 제한해 와이드 모니터에서 내용이
    과도하게 늘어나지 않게 했다.
- **장식용 글리프를 없앴다.** 브랜드 마크의 `◆` 를 텍스트 배지로 바꿨다.
  이모지와 장식 기호를 쓰지 않는다.
- **조회 전 차트 자리에 플레이스홀더를 넣었다.** 이전에는 큰 빈 박스만 남아
  고장으로 보였다.

## [1.4.0] - 2026-08-29

세 개의 독립 리뷰(운영자 / 사용자 / 보안·코드 관점)에서 나온 결함을 고친
릴리스다. 가장 중요한 것은 클라이언트가 헤더 하나로 사용량 집계와 월 예산을
우회할 수 있던 문제다.

### 수정

- **[중요] 클라이언트 `X-Request-Id` 로 사용량 집계와 월 예산을 우회할 수
  있던 문제.** 사용량 테이블의 정렬 키가 클라이언트가 지정하는
  `X-Request-Id` 였다. 같은 값을 계속 보내면 원본 쓰기의
  `attribute_not_exists` 조건이 실패해 **트랜잭션 전체가 취소**되고, 집계
  11개 항목이 하나도 반영되지 않았다. 예산 검사는 집계 테이블을 읽으므로
  누적이 늘지 않아 월 한도가 영원히 걸리지 않고, 청구 배분에서도 빠졌다.
  Bedrock 호출은 그때마다 실제 비용이 발생하므로 정직한 클라이언트의
  재시도에서도 두 번째 호출 비용이 집계되지 않는 과소 집계가 있었다.
  - 정렬 키를 서버가 요청마다 만드는 `usage_id` 로 바꿨다. 호출 횟수만큼
    기록되고 집계·예산에 반영된다.
  - 네트워크 재전송 보호는 `TransactWriteItems` 의 `ClientRequestToken`
    으로 옮겼다. 같은 트랜잭션이 두 번 도착해도 집계는 한 번만 더해진다.
  - `X-Request-Id` 는 상관관계 ID 로만 쓰이며 레코드의 `request_id` 속성과
    응답 헤더, 모든 로그에 그대로 남는다.
  - 회귀 테스트 2건과 스모크 테스트를 추가·전환했다. 스모크 7번 항목은
    이제 "집계 우회 방어" 를 검증한다(호출 2회 → 2건 기록).
- **액세스 로그에 `correlation_id` 가 빠지던 문제.** 미들웨어가 컨텍스트를
  되돌린 뒤에 로그를 남겨, `status_code` 와 `duration_ms` 를 가진 유일한
  로그를 요청 ID 로 조회할 수 없었다. 로깅을 `reset` 이전으로 옮겼다.
- **`/v1/models` 가 Converse 로 호출할 수 없는 모델을 노출하던 문제.**
  재순위(rerank)·임베딩·이미지 생성 모델이 목록에 섞여 있어, 그것을 고른
  클라이언트가 `ValidationException` 으로 400 을 받았다. 기반 모델과 추론
  프로파일 양쪽에 Converse 지원 여부 필터를 적용했다.
- **모델 `owned_by` 가 공급자가 아니라 리전 접두어로 나오던 문제.**
  `us.amazon.nova-lite-v1:0` 의 소유자가 `amazon` 이 아니라 `us` 였다.
  추론 프로파일 접두어를 제거한 뒤 공급자를 계산한다.

### 문서

- README 의 `cd` 경로를 실제 리포지토리 이름(`llm-gateway-bedrock`)으로
  고쳤다. 그대로 복붙하면 배포 시작 전에 막혔다.
- 관리 API 예시 ID 를 데모 시드와 겹치지 않는 값으로 바꿨다(`acme` →
  `contoso` 등). 기본 배포 직후 문서를 위에서부터 따라가면 첫 단계에서
  `409 already_exists` 로 막혔다. 데모 데이터 없이 시작하는 방법도 적었다.
- OpenAI SDK 예제에 설치 명령을 추가했다. SDK 는 이 리포지토리의 의존성이
  아니라 예제가 `ModuleNotFoundError` 로 시작했다.
- 잘못된 모델 ID 의 실제 응답(400 `invalid_request`)을 트러블슈팅 표에
  반영했다. 기존 표는 404 만 적어 재시도 로직을 잘못 짜게 했다.
- CONTRIBUTING 에 `shellcheck` OS 별 설치 방법을 넣었다. 검증 명령에는
  있었지만 설치 안내가 없었다.

## [1.3.0] - 2026-08-29

배포 스크립트가 템플릿의 파라미터를 완전히 노출하도록 맞추고, 오픈소스
프로젝트로서 갖춰야 할 커뮤니티 표준 파일을 더한 마이너 릴리스다. API 와
데이터 모델은 바뀌지 않는다.

### 추가

- **배포 CLI 옵션 3종.** `infra/app.yaml` 에는 있었지만 `deploy.sh` 가
  노출하지 않던 파라미터를 명령줄에서 지정할 수 있다.
  - `--bedrock-region` — Bedrock 호출 리전을 배포 리전과 분리한다. 게이트
    웨이는 서울에 두고 모델은 `us-east-1` 에서 부르는 구성이 가능하다.
    지정하면 사전 점검의 모델 액세스 확인도 그 리전에서 수행한다.
  - `--usage-ttl-days` — usage 원본 레코드 보존 기간(일).
  - `--allowed-model-arn` — 태스크 역할이 호출 가능한 기반 모델 ARN 패턴.
    IAM 을 특정 모델로 좁힐 때 쓴다.
- **커뮤니티 표준 파일.** `CONTRIBUTING.md`(개발 환경·검증 명령·PR 절차),
  `CODE_OF_CONDUCT.md`(Contributor Covenant 2.1), GitHub 이슈 템플릿(버그·
  기능), PR 템플릿, `dependabot.yml`(pip·github-actions·docker 주간 점검).

### 수정

- **문서-코드 불일치 해소.** README 는 이미 `--bedrock-region`,
  `--allowed-model-arn`, `--usage-ttl-days` 예시를 실었으나 스크립트가
  해당 인자를 받지 못해 "알 수 없는 인자" 로 실패하던 문제를 바로잡았다.
  빈 값을 넘기면 CloudFormation 기본값이 유지되도록 선택 파라미터를
  조건부로만 전달한다.
- **깨진 CI 액션 핀 교정.** `actions/upload-artifact@v4.6.1` 과
  `docker/build-push-action@v6.15.0` 의 커밋 SHA 가 손상돼 워크플로가 "Set
  up job" 단계에서 즉시 실패하던 것을 실제 릴리스 SHA 로 바로잡았다.
- **Node 22 하네스 호환.** `admin_ui_harness.js` 가 `global.navigator` 를
  직접 할당해, 이를 getter 전용으로 노출하는 Node 22+ 러너에서 `TypeError`
  로 실패하던 것을 `Object.defineProperty` 로 교정했다.

## [1.2.1] - 2026-08-29

관리 UI의 핵심 흐름을 실제 Chromium에서 검증하는 회귀 테스트를 추가한 패치
릴리스다. v1.2.0의 API와 데이터 모델은 바꾸지 않는다.

### 추가

- **Python Playwright 기반 브라우저 테스트.** 모니터링/관리 화면 전환, 계정
  CRUD 뒤 선택 목록 동기화, API 키 발급·재발급 평문 표시, 오류 뒤 폼 모달
  유지와 재시도, 데스크톱·모바일 기본 레이아웃을 실제 DOM·CSS·이벤트로
  검증한다. Playwright는 개발 의존성에만 포함된다.
- **Chromium 전용 CI 작업.** 브라우저 바이너리를 별도 작업에서 설치하고 관리
  UI 테스트만 실행한다. 기존의 빠른 Node DOM 하네스는 그대로 유지한다.

### 수정

- **숨겨진 모달이 화면 클릭을 가로막지 않는다.** `.modal-root`의
  `display: flex`가 HTML `hidden` 속성보다 우선해 초기 화면을 덮던 실제
  브라우저 CSS 문제를 `[hidden]` 규칙으로 바로잡았다.

## [1.2.0] - 2026-08-28

관리 API 를 완전한 CRUD 로 확장하고, 대시보드에 계정·팀·사용자·API 키를
직접 만들고 고치는 관리 화면을 얹었다. 이전에는 `curl` 로만 가능하던 관리
작업을 브라우저에서 처리할 수 있다.

### 추가

- **관리 API CRUD 완성.**
  - 계정: `PATCH /admin/accounts/{id}`(이름·예산 수정), `DELETE`(하위 리소스
    없을 때만).
  - 팀: `GET`/`PATCH`/`POST .../status`(활성 토글)/`DELETE` 추가.
  - 사용자: `GET`/`PATCH`/`POST .../status`/`DELETE` 추가.
  - API 키: `GET`/`PATCH`(이름·허용 모델·예산)/`POST .../status`/`POST
    .../rotate`(재발급) 추가.
  - 삭제는 참조 무결성을 강제한다. 하위 팀·사용자·키가 남아 있으면 `409`
    로 거부한다.
  - 부분 수정은 요청에 실린 필드만 반영한다. `monthly_budget_usd` 를 `null`
    로 보내면 예산을 무제한으로 되돌린다.
- **관리 대시보드 화면.** `/ui/` 에 "모니터링 / 관리" 전환을 추가했다. 관리
  화면에서 계정·팀·사용자·키를 폼으로 만들고 수정·삭제·상태토글·재발급한다.
  재발급·발급 시 평문 키를 한 번만 보여 준다. 기존과 같이 외부 라이브러리와
  빌드 단계 없이 동작한다.

### 수정

- **팀·사용자 비활성화 경로가 실제로 열렸다.** 인증기는 팀·사용자의 비활성
  상태를 검사하고 있었지만, 정작 이들을 비활성으로 만들 관리 API 가 없었다.
  status 토글 엔드포인트를 추가해 이 방어 로직에 도달할 수 있게 했다.
- **발급·재발급한 평문 키가 사라지지 않는다.** 폼 제출 후 공통 처리기가 방금
  연 키 표시 모달까지 닫아 버리는 문제를 고쳤다. 콜백이 이어서 모달을 열면
  자동 닫기를 건너뛴다.
- **모니터링 탭과 관리 탭의 이벤트 충돌을 없앴다.** 대시보드 탭 선택자를
  모니터링 화면 범위로 제한해, 관리 탭을 눌러도 모니터링 표 상태가 깨지지
  않는다.
- **키 재발급을 원자적 트랜잭션으로 만들었다.** 새 해시 생성과 옛 해시 삭제를
  하나의 `TransactWriteItems` 로 묶어, 중간 실패로 두 키가 동시에 유효해지거나
  같은 `key_id` 가 GSI 에 중복되는 상황을 막는다.
- **PATCH 가 문자열 필드에 `null` 을 저장하지 못하게 했다.** 이름·이메일·팀
  같은 문자열 필드는 `null` 을 거부한다. `null` 은 예산 필드에서만 허용되고,
  값을 비우려면 빈 문자열을 쓴다.
- **삭제된 팀·사용자를 참조하는 키를 fail-closed 로 거부한다.** 인증기가
  팀·사용자 레코드가 없으면(삭제되었거나 최종 일관성으로 아직 안 보이면)
  통과시키던 fail-open 을 고쳤다. 이제 계정·팀·사용자 중 하나라도 없거나
  비활성이면 거부하고, 존재 여부를 외부에 알리지 않도록 모두 같은 일반 인증
  오류를 반환한다.
- **키 재발급 트랜잭션에 작업별 토큰을 붙였다.** 각 저장소 호출에서 새
  `ClientRequestToken` 을 만들어, boto3 의 한 SDK 호출 안에서 일어나는 재시도가
  트랜잭션을 두 번 적용하지 않게 한다. 클라이언트가 같은 `X-Request-Id` 로
  HTTP 요청을 반복해도 서로 다른 트랜잭션끼리 토큰이 충돌하지 않는다. HTTP
  `/rotate` 재호출은 여전히 새 평문 키를 새로 발급한다.

### UI

- **계정 선택 목록이 관리 작업 직후 갱신된다.** 관리 화면에서 계정을 만들거나
  지우면 상단 계정 선택 목록이 즉시 최신 상태가 된다. 모니터링 화면을 다시
  조회할 필요가 없다.

- **관리 UI 동작 테스트를 추가했다.** `tests/js/admin_ui_harness.js` 가 최소
  DOM 셰임과 fetch 목 위에서 실제 클릭·제출 이벤트를 발생시켜, 평문 키 모달
  유지·모니터링/관리 탭 분리·계정 목록 동기화를 검증한다. charts 하네스와
  같은 방식이라 npm·브라우저 없이 Node 로만 돌아가고, Node 가 없으면
  건너뛴다.

### 변경

- **관리 삭제를 조건부 삭제로 바꿨다.** 계정·팀·사용자·API 키 삭제가
  `attribute_exists` 조건을 걸어, 조회-후-삭제 사이에 다른 요청이 이미 지운
  항목을 조용히 204 로 넘기지 않고 404 로 드러낸다. 부모 삭제 쪽 경쟁 창을
  좁힌다.
- **관리 갱신도 존재 조건을 확인한다.** 계정·팀·사용자 PATCH 및 상태 변경이
  선행 조회 뒤 삭제된 항목을 무조건 `PutItem` 으로 되살리지 않고 404 를
  반환한다.

### 알려진 한계 (후속 과제)

- **키 재발급은 HTTP 수준에서 멱등하지 않다.** `ClientRequestToken` 은 SDK 내부
  재시도만 보호한다. 응답 유실 후 `/rotate` 를 다시 부르면 또 새 평문 키가
  발급된다. "재시도 시 같은 평문 키 반환" 은 그 평문을 어딘가에 저장해야
  가능한데, 이는 **"평문 키는 저장하지 않는다"는 보안 보증**(README·SECURITY.md)
  과 정면으로 충돌한다. 또 registry 테이블에는 TTL 이 없어 멱등성 저장소를
  두려면 인프라 변경도 필요하다. 따라서 이 항목은 보안 보증을 완화하는 별도
  결정 없이는 구현하지 않는다. 현재는 재발급을 "다시 부르면 또 새 키가 나온다"
  는 동작으로 다룬다.
- **관리 삭제의 자식-생성 방향 경쟁은 남아 있다.** 부모 삭제 쪽은 조건부 삭제로
  좁혔지만, 하위 리소스 조회와 부모 삭제 사이에 자식이 새로 생기는 방향까지
  완전히 막지는 못한다. 인증이 fail-closed 라 고아 키의 보안 위험은 없다.
  완전한 차단은 soft delete 또는 참조 카운터가 필요하다.

## [1.1.0] - 2026-08-24

접근 통제를 다중 단말 운영에 맞게 재설계하고, 비용 집계의 조용한 누락을
드러나게 만들었다. Claude 연동과 Bedrock 엔드포인트 선택 근거를 문서로 남겼다.

0.1.0 에서 바로 1.1.0 으로 올렸다. 접근 통제 방식이 바뀌어 배포 인터페이스가
달라졌고, 사용량 집계에 필드가 추가되어 기능 증분이 패치 수준을 넘는다.

### 변경 (BREAKING)

- **접근 허용 CIDR 을 관리형 프리픽스 리스트로 관리한다.** ALB 보안 그룹이
  프리픽스 리스트 하나만 참조하므로 단말이 몇 개든 규칙은 프로토콜당 1개다.
  **단말 추가·삭제에 스택 재배포가 필요 없고 수 초 안에 반영된다.**
  - CloudFormation 파라미터 `AllowedIngressCidr1/2/3` 이 **제거**되었다.
    대신 `AccessListMaxEntries`(기본 20)가 추가됐다.
  - `deploy.sh` 의 `--allowed-cidr-2`, `--allowed-cidr-3` 옵션이 **제거**되었다.
    `--allowed-cidr` 를 여러 번 지정한다.
  - 기존 스택은 재배포 시 보안 그룹 규칙이 교체된다. 배포 직후 프리픽스
    리스트가 채워지므로 접근이 유지되지만, 배포 중 짧은 단절이 있을 수 있다.
  - 프리픽스 리스트는 **빈 상태로 생성**된다. 아무도 접근할 수 없는 상태에서
    시작하고 명시적으로 추가한 단말만 통과한다.
- **`0.0.0.0/0` 차단 지점이 이동했다.** 이전에는 CloudFormation 파라미터 정규식이
  막았다. 프리픽스 리스트에는 파라미터 제약을 걸 수 없어, 차단이 세 곳으로
  옮겨졌다: `deploy.sh` 검증, `manage_access.sh` 검증, 그리고 매 배포마다
  실행되는 `smoke_test.sh` 의 전체 개방 규칙 검사. `/8` 보다 넓은 대역도
  스크립트가 거부한다.
- **집계에 `unpriced_requests` 필드가 추가됐다.** `/analytics/*` 응답에
  `unpriced_requests` 와 `cost_complete` 가 포함된다. 응답 스키마에 필드가
  늘어난 것이므로 기존 클라이언트는 영향받지 않는다. 1.0 에서 기록된 집계
  행에는 이 속성이 없고 0으로 읽힌다.

### 추가

- **`scripts/manage_access.sh`** — 접근 단말 관리. `list`, `add`, `add-me`,
  `remove`, `check`, `status` 서브명령. 라벨로 어느 단말인지 기록한다.
  `check` 는 목록 확인과 실제 HTTP 호출을 함께 수행한다.
- **`scripts/scan_secrets.sh`** — 시크릿 검사. AWS 키, GitHub 토큰, Slack 토큰,
  OpenAI 키, 게이트웨이 API 키 평문, Bedrock API 키, private key, JWT, 실제 계정
  ID, 계정 ID 포함 ARN, 추적돼서는 안 되는 경로를 검사한다. `--history` 로 전체
  커밋 히스토리까지 확인한다. CI 잡으로 추가했다.
- **`scripts/sync_pricing.py`** — AWS Price List API 와 단가 표를 대조해 불일치·
  누락·미확인을 보고한다. `--apply` 로 **API 가 확인한 값만** 반영한다.
  확인되지 않은 모델의 단가를 추측해 넣지 않는다.
- **단가 미등록 사용량 가시화.** 단가 표에 없는 모델로 처리된 요청이
  `unpriced_requests` 로 집계되고, 대시보드 총비용 카드에
  `USD — 단가 미등록 N건 제외됨` 경고가 표시된다. 이전에는 비용이 0으로 조용히
  누락됐다.
- **`docs/models-claude.md`** — Claude 연동 상세. 현행 Claude 가 전부
  `INFERENCE_PROFILE` 전용이라 기반 모델 ID 로는 호출되지 않는다는 점,
  `us.` 와 `global.` 선택 기준, 허용 목록 정규화가 프로파일 접두어를 무시한다는
  점과 그 보안 함의, LEGACY/EOL 처리, 단가 갭의 현재 상태와 메우는 방법,
  미지원 기능(비전·도구 사용·프롬프트 캐싱 등) 목록, 실측 검증 결과.
- **`docs/bedrock-endpoints.md`** — `bedrock-runtime` 과 `bedrock-mantle` 전체
  비교(API·기능·인증·처리량·요금), 게이트웨이가 `bedrock-runtime` + Converse 를
  고른 이유, **AWS 가 이미 OpenAI 호환 API 를 제공하는데 이 게이트웨이가 필요한
  이유와 필요 없는 경우**, Mantle 전용 기능이 필요할 때의 경로, Guardrails 도입
  설계 지점, PrivateLink 손익분기 계산.
- **`SECURITY.md`** — 시크릿 관리 원칙, 검사 절차, 유출 시 대응 순서, 접근 통제
  3계층, 알려진 위험 4건, IAM 와일드카드 사유, 데이터 보호 설정, 프로덕션 전환
  보안 체크리스트.
- **`tests/test_static_charts.py`** — 대시보드 SVG 차트 렌더링 검증 15건.
  최소 DOM 셰임(`tests/js/charts_harness.js`)을 Node 로 실행한다. 경계 케이스
  (빈 데이터, 데이터 1건, 값 전부 0, 조각 1개 도넛)와 접근성 속성을 확인한다.
  Node 가 없으면 건너뛴다. npm 의존성은 추가하지 않았다.
- **모델 단가 17종 추가.** Price List API 로 확인된 값만 반영했다.
  DeepSeek v3.2, MiniMax M2 계열, Kimi K2 계열, Nemotron 3, GPT-OSS Safeguard,
  Qwen3 계열, GLM 4.7/5, Palmyra Vision 등. 단가 표가 23 → 40개가 됐다.

### 검증

- 단가 표 대조 결과 **기존 23개 항목이 AWS Price List API 값과 불일치 0건**.
  손으로 넣은 스냅샷이 정확했음이 확인됐다.
- 프리픽스 리스트 설계는 실제 스택 업데이트로 검증했다. `Entries` 속성을
  템플릿에서 생략하면 CloudFormation 이 스택 밖에서 추가한 엔트리를 되돌리지
  않는다.
- 시크릿 검사: 워킹트리·전체 커밋 히스토리·커밋 메시지·원격 브랜치 모두 클린.

### 알려진 제약 (변경 사항)

- **현행 Claude 모델의 단가가 등록되지 않았다.** 이 계정의 Price List API 에는
  레거시 Claude 5종만 존재해 자동 확인이 불가능했다. Sonnet 4.5~5,
  Opus 4.5~5, Haiku 4.5, Fable 5 의 비용이 0으로 집계된다. 추측한 값을 넣지
  않은 것은 의도적이다. 이 상태는 `unpriced_requests` 와 대시보드 경고로
  드러난다. **단가가 없는 모델에 예산을 걸면 예산이 작동하지 않는다.**
  메우는 방법은 `docs/models-claude.md` 5절에 있다.
- 프리픽스 리스트 엔트리는 CloudFormation 이 관리하지 않는다. 재배포 없이
  단말을 바꿀 수 있는 이유이면서, AWS CLI 로 직접 넓은 대역을 넣는 것이
  가능하다는 뜻이다. 스모크 테스트의 검사가 마지막 그물이다.
- `AccessListMaxEntries` 는 생성 후 늘릴 수만 있고 줄일 수 없다.

## [0.1.0] - 2026-08-24

첫 릴리스.

### 추가

**게이트웨이**

- OpenAI 호환 `POST /v1/chat/completions`. 비스트리밍과 SSE 스트리밍 모두 지원
- `GET /v1/models`. API 키의 허용 목록으로 필터링해 반환
- OpenAI ↔ Bedrock Converse 변환. 시스템 메시지 분리, 연속 동일 역할 병합,
  `finish_reason` 매핑
- `X-Request-Id` 기반 멱등성. 재시도해도 사용량이 중복 집계되지 않는다

**접근 통제**

- API 키 인증. 평문은 저장하지 않고 SHA-256 해시로만 조회
- 계정 → 팀 → 사용자 → 키 4단 계층
- 키별 모델 허용 목록. 추론 프로파일 접두어(`us.` 등)를 정규화해 비교
- 계정·팀·사용자·키 4단계 월 예산. 하나라도 초과하면 429
- 계정·팀·사용자·키 활성 상태 전환

**사용량 기록**

- 요청 단위 원본 기록과 5축(전체/팀/사용자/모델/키) × 2기간(일/월) 사전 집계
- 원본 쓰기와 집계 갱신을 단일 `TransactWriteItems` 로 처리. 멱등성이 저장소
  제약으로 보장된다
- 모델 단가 표 기반 비용 계산. 표에 없는 모델은 비용 0과 `pricing_known=false`
  플래그로 기록하고 경고 로그를 남긴다

**관리 API**

- 계정·팀·사용자·API 키 생성과 조회, 키 삭제, 계정 상태 변경
- `GET /admin/models`. 단가 인지 여부를 함께 반환

**집계 조회 API**

- `/analytics/summary`, `/timeseries`, `/breakdown`, `/requests`, `/accounts`
- `/analytics/dashboard`. 대시보드 한 화면 데이터를 한 번에 반환

**대시보드 UI**

- KPI 카드 5개, 차트 4개, 상세 표 6개 탭 (계정/팀/사용자/모델/키/최근 요청)
- 기간 프리셋과 임의 구간 조회, 30초 자동 새로고침
- 외부 의존성 없는 SVG 차트 자체 구현
- 관리 토큰은 `sessionStorage` 에만 저장

**인프라**

- CloudFormation 2스택. VPC, ALB, ECS Fargate, DynamoDB 3개, IAM, Secrets
  Manager, CloudWatch 알람 4개, SNS, 오토스케일링
- 접근 CIDR 필수 파라미터. `0.0.0.0/0` 과 모든 `/0` 프리픽스를 정규식으로 거부
- 배포 서킷 브레이커 + 자동 롤백
- S3·DynamoDB 게이트웨이 엔드포인트 (무료). NAT Gateway 미사용
- 관리 토큰을 CloudFormation 이 생성해 Secrets Manager 에 저장

**운영 도구**

- `scripts/deploy.sh`. 사전 점검부터 스모크 테스트까지 원커맨드 배포
- `scripts/teardown.sh`. 단계별 삭제와 `--purge-data`
- `scripts/seed_demo_data.py`. 데모 조직 구조 생성과 실제 사용량 발생
- `scripts/smoke_test.sh`. 인증 경계, 스트리밍, 멱등성, 집계 축, 비용 계산 검증
- `scripts/export_openapi.py` + 드리프트 검증 테스트

**관측**

- JSON 구조화 로그. 모든 줄에 `correlation_id` 자동 첨부
- EMF 커스텀 메트릭 6종 (`Requests`, `Errors`, `InputTokens`, `OutputTokens`,
  `CostUsd`, `LatencyMs`) + `UsageWriteFailures`
- `/healthz`(얕음, ALB 용)와 `/readyz`(의존성 확인) 분리

### 알려진 제약

- **분산 트레이싱이 없다.** 구간별 지연 분해가 불가능하다. 근거와 도입 절차는
  [ADR 0004](docs/adr/0004-region-and-observability.md) 에 있다
- **DynamoDB `DeletionPolicy` 가 `Delete` 다.** 프로덕션 전환 시 `Retain` 으로
  바꿔야 한다
- **인증서 없이 배포하면 HTTP 로만 서비스한다.** API 키가 평문 전송된다
- **집계 축이 고정이다.** 5축 외의 조합(예: 팀 내 사용자별 모델 분포)은 원본을
  읽어야 한다
- **최대·백분위 지연이 집계 테이블에 없다.** `ADD` 로 최댓값을 누적할 수 없어
  평균만 두고 백분위는 CloudWatch 에서 본다
- **한 계정의 쓰기 처리량에 상한이 있다.** 집계 파티션이 `계정#기간` 이라 파티션당
  1,000 WCU 제약을 받는다
- **모델 단가는 스냅샷이다.** `src/llmgw/pricing.json` 을 주기적으로 대조해야 한다
- **이미지가 x86_64 다.** arm64(Graviton)가 비용 효율이 낫지만 빌드 호스트
  아키텍처에 맞췄다
- **메타데이터 캐시 TTL 30초.** 계정·팀·사용자 상태 변경이 그만큼 지연 반영된다.
  키 삭제는 즉시 적용된다
